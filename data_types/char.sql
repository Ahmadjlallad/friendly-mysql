-- char is a fixed charactares
-- length is fix when we declare a table
-- when store if it less than the spcifed char it will add spaces
-- but when retreve them sql will remove it
-- password
-- decimal
create Table a(c DECIMAL(5, 2)) -- 5 max number of left number 2 right